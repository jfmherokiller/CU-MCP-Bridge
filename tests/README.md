# CU-MCP-Bridge Tests
#
# C# tests require .NET Framework 4.7.2:
#   cd tests\csharp && dotnet test
#
# Python tests require pytest:
#   cd tests\python && pytest -v
#
# Integration tests require both C# mod and Python server running.
# See opencode_mcp.json for server configuration.
